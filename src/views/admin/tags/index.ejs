<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tags - East Village Everything Admin</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .tags-table th a {
      color: inherit;
      text-decoration: none;
    }
    .tags-table th a:hover {
      text-decoration: underline;
    }
    .sort-arrows {
      font-size: 0.75rem;
      margin-left: 4px;
    }
    .btn-actions {
      white-space: nowrap;
    }
    .btn-actions .btn {
      margin-right: 4px;
    }
    .btn-actions .btn:last-child {
      margin-right: 0;
    }

    /* Mobile card styles */
    .tag-cards {
      display: none;
    }
    .tag-card {
      background: #fff;
      border: 1px solid #dee2e6;
      border-radius: 4px;
      margin-bottom: 1rem;
    }
    .tag-card table {
      margin-bottom: 0;
    }
    .tag-card th {
      width: 100px;
      background-color: #f8f9fa;
      font-weight: 600;
    }
    .tag-card td, .tag-card th {
      padding: 0.5rem 0.75rem;
      border-color: #dee2e6;
    }

    /* Responsive breakpoint */
    @media (max-width: 768px) {
      .tags-table-wrapper {
        display: none;
      }
      .tag-cards {
        display: block;
      }
      .page-header {
        flex-direction: column;
        align-items: flex-start !important;
        gap: 1rem;
      }
    }
  </style>
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
    <div class="container">
      <a class="navbar-brand" href="/admin">EVE Admin</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav me-auto">
          <li class="nav-item">
            <a class="nav-link" href="/admin/places">Places</a>
          </li>
          <li class="nav-item">
            <a class="nav-link active" href="/admin/tags">Tags</a>
          </li>
        </ul>
        <% if (user) { %>
        <span class="navbar-text me-3"><%= user.name %></span>
        <form action="/admin/logout" method="POST" class="d-inline">
          <button type="submit" class="btn btn-outline-light btn-sm">Logout</button>
        </form>
        <% } %>
      </div>
    </div>
  </nav>

  <div class="container">
    <div class="d-flex justify-content-between align-items-center mb-4 page-header">
      <h1>Tags</h1>
      <a href="/admin/tags/new" class="btn btn-success">New Tag</a>
    </div>

    <%
      function sortUrl(column) {
        const newOrder = (sortBy === column && sortOrder === 'asc') ? 'desc' : 'asc';
        return '/admin/tags?sort_by=' + column + '&sort_order=' + newOrder;
      }
      function sortArrows(column) {
        if (sortBy !== column) return '<span class="sort-arrows text-muted">↑ ↓</span>';
        return sortOrder === 'asc'
          ? '<span class="sort-arrows">↑ <span class="text-muted">↓</span></span>'
          : '<span class="sort-arrows"><span class="text-muted">↑</span> ↓</span>';
      }
      function formatDate(dateStr) {
        if (!dateStr) return '';
        const d = new Date(dateStr);
        const months = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'];
        const month = months[d.getMonth()];
        const day = d.getDate().toString().padStart(2, '0');
        const year = d.getFullYear();
        let hours = d.getHours();
        const ampm = hours >= 12 ? 'pm' : 'am';
        hours = hours % 12 || 12;
        const minutes = d.getMinutes().toString().padStart(2, '0');
        return month + ' ' + day + ', ' + year + ' ' + hours + ':' + minutes + ampm;
      }
    %>

    <!-- Desktop Table View -->
    <div class="tags-table-wrapper">
      <table class="table table-striped table-bordered tags-table">
        <thead class="table-light">
          <tr>
            <th style="width: 120px;">Actions</th>
            <th style="width: 80px;">
              <a href="<%- sortUrl('sort_order') %>">Order <%- sortArrows('sort_order') %></a>
            </th>
            <th>
              <a href="<%- sortUrl('value') %>">Value <%- sortArrows('value') %></a>
            </th>
            <th>
              <a href="<%- sortUrl('display') %>">Display Name <%- sortArrows('display') %></a>
            </th>
            <th>
              <a href="<%- sortUrl('updated_at') %>">Updated At <%- sortArrows('updated_at') %></a>
            </th>
          </tr>
        </thead>
        <tbody>
          <% tags.forEach(function(tag) { %>
          <tr>
            <td class="btn-actions">
              <a href="/admin/tags/<%= tag.id %>/edit" class="btn btn-sm btn-success">Edit</a>
              <a href="/admin/tags/<%= tag.id %>/delete" class="btn btn-sm btn-danger">Delete</a>
            </td>
            <td><%= tag.sort_order %></td>
            <td><code><%= tag.value %></code></td>
            <td><%= tag.display %></td>
            <td><%= formatDate(tag.updated_at) %></td>
          </tr>
          <% }); %>
        </tbody>
      </table>
    </div>

    <!-- Mobile Card View -->
    <div class="tag-cards">
      <% tags.forEach(function(tag) { %>
      <div class="tag-card">
        <table class="table table-bordered">
          <tbody>
            <tr>
              <th>Actions</th>
              <td class="btn-actions">
                <a href="/admin/tags/<%= tag.id %>/edit" class="btn btn-sm btn-success">Edit</a>
                <a href="/admin/tags/<%= tag.id %>/delete" class="btn btn-sm btn-danger">Delete</a>
              </td>
            </tr>
            <tr>
              <th>Order</th>
              <td><%= tag.sort_order %></td>
            </tr>
            <tr>
              <th>Value</th>
              <td><code><%= tag.value %></code></td>
            </tr>
            <tr>
              <th>Display</th>
              <td><%= tag.display %></td>
            </tr>
            <tr>
              <th>Updated At</th>
              <td><%= formatDate(tag.updated_at) %></td>
            </tr>
          </tbody>
        </table>
      </div>
      <% }); %>
    </div>

    <% if (tags.length === 0) { %>
    <p class="text-muted">No tags yet. <a href="/admin/tags/new">Add one</a>.</p>
    <% } %>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
